  <table>
    <tr>
      <% product.photos.each do |photo| %>
        <td class='mx-2'>
          <%# link_to image_tag(photo, size:96), rails_blob_path(photo, disposition: 'preview'), target: :_blank  %>
          <%= image_tag(photo, size:96)  %>
        </td>
      <% end %>
    </tr>
  </table>
