<% if notice %>
  <div class='<%= notice["class"] %>'>
    <b><%= notice["message"] %></b>
  </div>
<% end %>

<p>
  <strong><%= "#{@kind.class.human_attribute_for :name}:" %></strong>
  <%= @kind.name %>
</p>

<p>
  <strong><%= "#{@kind.class.human_attribute_for :description}:" %></strong>
  <%= @kind.description %>
</p>

<%= link_to t('navigation.links.edit'), edit_kind_path(@kind) %> |
<%= link_to t('navigation.links.back'), kinds_path %>
